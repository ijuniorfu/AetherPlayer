import SwiftUI

@main
struct AetherPlayerApp: App {
    var body: some Scene {
        Window("AetherPlayer", id: "main") {
            Text("AetherPlayer")
                .frame(minWidth: 640, minHeight: 360)
        }
    }
}
